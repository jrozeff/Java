// This program demonstrartes how the user nay specify an array's size 

import java.util.Scanner;

public class DisplayTestScores {
	public static void main (String [] args) {
		int numTests;
		int[] tests;

		// Create a Scanner object for keyboard input
		Scanner keyboard = new Scannr(System.in);

		// Get the number of test scores
		System.out.print("How many tests do you have? ");
		numTests = keyboard.nextInt();

		// Create an array to hold that number of scores 
		tests = new int[numTests];

		// Get the individual test scores
		for (int index = 0; index < tests.length; index++) {
			System.out.print("Enter test score " + );
		}

	}
}